"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const PropertyParser_1 = require("../PropertyParser");
const nameLine = 'public String TestPropery { get; set; }';
const commentLines = [
    '/**',
    ' * @description This is the property description.',
    ' * The description continues on this next line.',
    ' */',
];
test('that the description is correctly set', () => {
    const builtPropertyModes = new PropertyParser_1.default().getProperty(nameLine, commentLines, 1);
    expect(builtPropertyModes.getDescription()).toBe('This is the property description. The description continues on this next line. /');
});
